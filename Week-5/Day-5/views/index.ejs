<!DOCTYPE html>
<html>
  <head>
    <title>Authentication System With Passport</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        background-image: linear-gradient(to top, #08635f, #e2e2e2);
        overflow: hidden;
      }

      .card {
        width: 90%;
        max-width: 400px;
        padding: 20px;
        background-color: #e2e2e2;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s ease-in-out;
      }

      .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
      }

      h1 {
        color: #333;
        margin-top: 0;
        text-align: center;
        margin-bottom: 20px;
        font-size: 24px;
        text-transform: uppercase;
      }

      form {
        margin-top: 20px;
      }

      button[type="submit"] {
        background-color: #245a63;
        color: white;
        padding: 12px 20px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 16px;
        font-weight: bold;
        text-transform: uppercase;
        width: 100%;
        transition: background-color 0.3s ease-in-out;
      }

      button[type="submit"]:hover {
        background-color: #5058b4;
      }

      a {
        color: #333;
        text-decoration: none;
        display: block;
        text-align: center;
        margin-top: 20px;
        font-size: 14px;
        font-size: 18px;
      }

      a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <div class="card">
      <h1>Hey <%= name %></h1>
      <form action="/logout?_method=DELETE" method="POST">
        <button type="submit">Log Out</button>
      </form>
    </div>
  </body>
</html>
